-- =====================================================================
-- 足跡 (Ashiato) — 0012: UI言語の保存先
-- =====================================================================
-- en / ja / ko / zh-Hans / zh-Hant。端末をまたいで言語設定を引き継ぐ。

alter table profiles add column if not exists language text;
